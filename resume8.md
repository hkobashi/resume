### プロジェクト概要
自社サービスのMySQLデータおよびSalesforceの営業・経営データをBigQueryに連携し、データ活用部門がデータ分析できる基盤を構築した。

### 使用技術
- `言語`：Python、Go
- `実行基盤`：Argo-Workflow
- `データ連携`：trocco（primeNumber）
- `データウェアハウス`：Google BigQuery
- `データソース`：MySQL、Salesforce

### チーム情報
- データ活用部門 9名  
→ 要件定義・設計・ETLスクリプト開発・運用改善まで一人で担当

### 担当業務
- `ETLパイプラインの設計・開発`  
  MySQL（自社サービスの投稿データ・アクティビティログ）および  
  Salesforce（営業・経営データ）からBigQueryへのデータ連携パイプラインを設計・開発した。  
  MySQLはPythonスクリプトで実装後、Goへリプレース。SalesforceはPythonスクリプトで実装。
- `Argo-Workflowによる実行基盤の整備`  
  ETLバッチの実行・スケジューリング基盤としてArgo-Workflowを整備した。
- `データ品質改善`  
  運用フェーズでの重複データ・欠損データ問題に対し、以下の改善を実施した。  
  ① 増分ロード範囲をPrimary Key（単調増加）で明確化し、ロードの断面を一意に定義  
  ② BigQueryテーブルに対して`CREATE OR REPLACE TABLE AS SELECT DISTINCT`で重複レコードを排除する処理を追加  
  ③ BigQuery・MySQL間のデータ件数突合スクリプトを追加し、重複・欠損を自動検知できる仕組みを構築

### どんな課題があったのか
- `運用フェーズでのデータ品質の安定化`  
  MySQLのバージョンアップによるPythonスクリプトのエラー、および  
  Argo-Workflowのリトライ制御とスクリプト処理の不備により、BigQueryへの重複データ混入が発生していた。

### どうやって課題を解決したのか
- Pythonスクリプトの修正によるMySQL互換性の対応、およびGoへのリプレースにより安定性を向上させた。
- 増分ロードの境界をPKで定義し直すことで重複の根本原因を排除。  
  加えて`CREATE OR REPLACE TABLE AS SELECT DISTINCT`による重複排除処理と  
  データ件数突合スクリプトを追加し、多層的なデータ品質保証の仕組みを構築した。

### 工夫した点
- `PKベースの増分ロード境界定義`  
  タイムスタンプベースの増分ロードは同一時刻のレコードで重複・欠損が発生するリスクがあるため、  
  単調増加するPrimary Keyを境界値として使用し、断面を一意に定義した。
- `多層的なデータ品質保証`  
  重複排除処理（`CREATE OR REPLACE TABLE AS SELECT DISTINCT`）と  
  データ件数突合スクリプトを組み合わせることで、重複の事後排除と欠損の自動検知を両立した。
